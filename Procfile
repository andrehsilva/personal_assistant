# Adiciona o novo arquivo
git add Procfile

# Cria um commit com uma mensagem clara
git commit -m "Adiciona Procfile para especificar comando de início"

# Envia para o ramo master (ou main)
git push origin master